# Vocab App

A small React + Vite TypeScript app for studying vocabulary with flashcards, quizzes, reviews, and progress tracking. Uses localStorage for persistence.

Commands:

- npm install
- npm run dev
- npm run test

